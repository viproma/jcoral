cmake_minimum_required(VERSION 3.0.0)
add_library(jdsb MODULE "native.cpp")
add_dependencies(jdsb headers)
target_include_directories(jdsb PRIVATE "${jniHeaderDir}" ${JNI_INCLUDE_DIRS})
target_link_libraries(jdsb ${JAVA_JVM_LIBRARY} dsb)
